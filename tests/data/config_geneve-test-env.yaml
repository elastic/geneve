stacks:
- name: geneve-test-env
  elasticsearch:
    hosts: $TEST_ELASTICSEARCH_URL
    api_key: ${TEST_API_KEY:-}
    ca_certs: ${TEST_CA_CERTS:-}
  kibana:
    url: $TEST_KIBANA_URL
    api_key: ${TEST_API_KEY:-}
    ca_certs: ${TEST_CA_CERTS:-}
